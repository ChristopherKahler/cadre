__pycache__/
*.py[cod]
*$py.class
*.egg-info/
*.egg
.pytest_cache/
.coverage
htmlcov/
dist/
build/
.venv/
venv/
env/
.tox/
.mypy_cache/
.ruff_cache/

.firm/

# BASE tooling cache (regenerated on demand)
.base-ast-cache/

# PAUL orchestration (internal project management)
.paul/

# Pre-build design docs (source material, not release artifacts)
ENTITY-DESIGN.md
LANDSCAPE.md
MEMBERS-DESIGN.md
PLANNING.md

# Tests (development only)

# Written by scripts/acceptance-e2e.py --json and by the acceptance job in
# tests.yml. Both land in the repo root, so running the harness locally the
# way CI runs it would otherwise leave two untracked files behind each time.
acceptance.json
acceptance-rc.txt
