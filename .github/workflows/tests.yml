# Cross-platform portability net — every push proves the suite on all three
# OSes, and all three now run the whole suite.
#
# Until 2026-09-10 the Windows job ran a -k filter that deselected 1081 of
# 1321 tests, with a comment saying the POSIX-flavored tests were "ported
# incrementally". Nobody could say which 1081, or why, or whether the number
# was moving. It was also hiding failures rather than only skipping tests:
# measured on Windows 10 / Python 3.12.6, the unfiltered suite failed 15 while
# the filtered one reported 3. The Windows leg had been red on every run for a
# month and nobody reported it, because the previous round graded the
# clean-install job and not the workflow as a whole.
#
# Windows support was never 18%. It was 98.9%, with a handful of real defects
# the filter was keeping out of sight. The tests that genuinely cannot run on
# a platform now carry an explicit skip naming the defect they wait on, listed
# in tests/platform_marks.py, so the count is a number anyone can read.
#
# The clean-install job below is the one that has to stay. See issue #3: a
# dependency with no upper bound resolved to a major version that removed a
# module Cadre imports, so `pip install cadre` was broken while every editable
# development environment stayed green, because each one had frozen the old
# version months earlier. An editable install in a warm environment cannot see
# that class of break. A wheel installed into an empty environment can.
name: tests

on:
  push:
    branches: [main, "feat/**", "lane/**", "infra/**"]
  pull_request:

jobs:
  test:
    name: suite (${{ matrix.os }})
    strategy:
      fail-fast: false
      matrix:
        os: [ubuntu-latest, macos-latest, windows-latest]
    runs-on: ${{ matrix.os }}
    steps:
      # Full history, not the depth-1 default. The runner fetches exactly one
      # ref, named after the branch that was pushed:
      #   fetch --no-tags --prune --depth=1 origin +<sha>:refs/remotes/origin/<branch>
      # So on a push to main, origin/main exists; on a push to a lane branch it
      # does not exist at all. tests/services/test_base_extension.py derives the
      # dependency guard from origin/main and skips when no baseline ref
      # resolves, so without this the guard would never actually run on the
      # branches it exists to guard — green, and checking nothing.
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1  # v7.0.1
        with:
          fetch-depth: 0
      - uses: actions/setup-python@5fda3b95a4ea91299a34e894583c3862153e4b97  # v7.0.0
        with:
          python-version: "3.12"
      - name: Install
        run: python -m pip install -e ".[dev]" || python -m pip install -e . pytest
      # -o addopts=... overrides pyproject's own "-ra -q" rather than adding
      # to it. Passing -q here as well gives pytest two of them, and the second
      # suppresses the summary line. The count was never truly lost, since it
      # can be recovered by counting the progress characters, but nobody should
      # have to count dots to learn whether a suite passed.
      - name: Full suite (POSIX)
        if: runner.os != 'Windows'
        run: python -m pytest tests -o addopts="-ra"
        env:
          PYTHONPATH: src
          # The spawn layer refuses to run without a wired claude runtime —
          # a real guard in production, a stub in CI (every spawn is mocked).
          CADRE_CLAUDE_BIN: /bin/echo
      # Same suite, same flags. The only difference is the stub runtime, and
      # PYTHONIOENCODING left unset on purpose: setting it hands every child
      # process a UTF-8 stdout for free and hides exactly the locale defects
      # this leg exists to catch.
      - name: Full suite (Windows)
        if: runner.os == 'Windows'
        run: python -m pytest tests -o addopts="-ra"
        env:
          PYTHONPATH: src
          CADRE_CLAUDE_BIN: C:\Windows\System32\cmd.exe

  action-pins:
    name: actions are pinned to commits
    # Lives in this workflow deliberately, so it inherits BOTH triggers: push on
    # main/feat/lane/infra AND every pull_request. A job wired to push alone
    # never shows on a builder branch outside that list, which is most of them.
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1  # v7.0.1
      - uses: actions/setup-python@5fda3b95a4ea91299a34e894583c3862153e4b97  # v7.0.0
        with:
          python-version: "3.12"
      # No continue-on-error. main has no branch protection (measured
      # 2026-09-10: GET /branches/main/protection is 404, rulesets empty), so
      # nothing in GitHub blocks a merge on a red check. What blocks a merge is
      # the repo's own gate reading check runs, and it can only read a run that
      # actually failed.
      - run: python scripts/check-action-pins.py
      # A SEPARATE step, deliberately not chained behind the one above with
      # `&&`. A non-zero exit swallowed by a chain is a check the merge gate
      # cannot see go red, and #52 removed continue-on-error from this job for
      # the same reason. check-action-pins.py checks the SHAPE of every pin;
      # this checks that docs/ACTION-PINS.md still describes those pins.
      - run: python scripts/check-pins-doc.py

  clean-install:
    name: clean install from the built wheel
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1  # v7.0.1
      - uses: actions/setup-python@5fda3b95a4ea91299a34e894583c3862153e4b97  # v7.0.0
        with:
          python-version: "3.12"

      - name: Build the wheel and the source distribution
        run: |
          python -m pip install --upgrade build
          python -m build
          ls -l dist/

      - name: Install the wheel into an empty environment
        run: |
          python -m venv /tmp/clean
          /tmp/clean/bin/python -m pip install --upgrade pip
          /tmp/clean/bin/python -m pip install dist/*.whl
          echo "--- what actually resolved ---"
          /tmp/clean/bin/python -m pip freeze

      # The discriminator. Nothing here adds src to the path, so every import
      # below resolves out of site-packages. A dependency that installs but
      # cannot be imported fails on this step and nowhere else.
      - name: Prove the installed package imports
        run: |
          /tmp/clean/bin/python - <<'PY'
          import importlib
          # firm.mcp.tools is the canary: it is the module that a major version
          # bump of the mcp dependency broke, and it imports at package load.
          for name in ("firm", "firm.mcp.tools", "firm.pulse.spawn",
                       "firm.dashboard.server", "firm.services.base_domain"):
              importlib.import_module(name)
              print("imported", name)
          PY

      - name: Prove the console scripts resolve
        run: |
          /tmp/clean/bin/cadre --version
          /tmp/clean/bin/firm --version

      - name: Run the suite against the installed package
        run: |
          /tmp/clean/bin/python -m pip install pytest
          /tmp/clean/bin/python -m pytest tests -o addopts="-ra"
        env:
          CADRE_CLAUDE_BIN: /bin/echo

      # `cadre init` requires the workspace directory to exist already, by
      # design. Creating it here is not incidental setup: without the mkdir this
      # step fails with "workspace does not exist", which reads exactly like a
      # broken package and is not one. That misreading cost a round of
      # investigation before this job was ever merged.
      - name: Initialize a firm from the installed package
        run: |
          mkdir -p /tmp/smoke-firm
          /tmp/clean/bin/cadre init /tmp/smoke-firm --demo
          test -f /tmp/smoke-firm/.firm/firm.db
          echo "firm database written by the installed package"

      # Package data is separate from importable code and fails separately:
      # a wheel can import perfectly and still ship no migrations, in which
      # case every install is broken the first time it touches a database.
      - name: Prove the wheel carries its data files, not only its code
        run: |
          /tmp/clean/bin/python - <<'PY'
          import pathlib, firm
          root = pathlib.Path(firm.__file__).parent
          checks = {
              "migrations": sorted(root.glob("migrations/*.sql")),
              "dashboard html": sorted(root.glob("dashboard/*.html")),
              "templates": sorted(root.glob("templates/**/*.md")),
          }
          missing = [name for name, found in checks.items() if not found]
          for name, found in checks.items():
              print(f"{name}: {len(found)}")
          if missing:
              raise SystemExit("wheel is missing packaged data: " + ", ".join(missing))
          PY

      - uses: actions/upload-artifact@043fb46d1a93c77aae656e7c1c64a875d1fc6a0a  # v7.0.1
        with:
          name: dist
          path: dist/
          retention-days: 14

  # Issue #49. scripts/acceptance-e2e.py is the instrument behind this project's
  # headline claim -- can a real user install Cadre and USE it, on this OS -- and
  # until this job existed, no workflow ran it. That scoreboard was only ever
  # true while somebody had recently typed the command by hand.
  #
  # What that costs is checked in one file over. scripts/e2e-test.sh:274 asserts
  # a fixed MCP tool count, and its own comment records that the number had
  # DRIFTED, because nothing executed the assertion between hand-runs. It is the
  # same fault family as a hook that is registered and cannot run (#40), or an
  # install() that reads its own TOML back instead of running the handler (#44):
  # a check that exists, looks authoritative, and is never executed in the way
  # that would make it authoritative.
  #
  # Standalone beside test, action-pins and clean-install, so it inherits both
  # triggers at the top of this file with no second trigger list. Measured on
  # PR #52: the sha carrying the standalone action-pins job produced ten check
  # runs where every previous sha produced eight, green on the push suite and on
  # the pull request suite, so a job placed here is visible to the merge gate.
  acceptance:
    name: acceptance (${{ matrix.os }})
    strategy:
      fail-fast: false
      matrix:
        os: [ubuntu-latest, macos-latest, windows-latest]
    runs-on: ${{ matrix.os }}
    # The harness builds a wheel, makes a virtual environment and drives a whole
    # firm. Measured 2026-09-10 at 842d8bc on a warm developer box: 22s on Linux,
    # 62s on Windows. The ceiling is here so that a child wedged behind the
    # harness's own 900s per-subprocess timeout cannot hold a runner for the
    # six-hour default.
    timeout-minutes: 30
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1  # v7.0.1
      - uses: actions/setup-python@5fda3b95a4ea91299a34e894583c3862153e4b97  # v7.0.0
        with:
          python-version: "3.12"

      # Nothing installs the project first and nothing puts src on the path. The
      # harness builds a wheel, installs it into an empty virtual environment and
      # drives the journey against the INSTALLED package, carrying its own
      # control that firm resolved from that environment rather than from this
      # checkout. Installing the project here would defeat the measurement.
      #
      # PYTHONIOENCODING stays unset on purpose, exactly as on the Windows suite
      # leg above: setting it hands every child process a UTF-8 stdout for free
      # and hides the locale defects this run exists to catch.
      #
      # This step decides nothing. It records the exit code and moves on. If both
      # this step and the grader could turn the job red, the job's colour would
      # come from whichever fired first, which is the same
      # which-check-actually-decided-this ambiguity that #49 is about.
      - name: Run the acceptance harness
        shell: bash
        run: |
          set +e
          python scripts/acceptance-e2e.py --json acceptance.json
          rc=$?
          set -e
          echo "$rc" > acceptance-rc.txt
          echo "harness exit code $rc -- the grading step below is the one that decides"

      # The single decision point. A FAIL row reds the build. BLOCKED and SKIP
      # rows are printed with the reason they carry and do not red it, because a
      # genuinely-pending row is information, and a SKIP that gets swallowed is
      # indistinguishable from a row that quietly stopped being emitted.
      #
      # Both inputs, never one of them. acceptance-e2e.py writes its JSON from a
      # finally: block, so a harness that dies half way through still leaves a
      # summary saying 0 fail. The exit code alone misses a report that stopped
      # telling the truth; the summary alone misses the crash.
      #
      # --min-rows is a FLOOR against a scoreboard that collapses, not an
      # equality check: rows that disappear rather than fail are invisible to any
      # FAIL count. Measured 2026-09-10 at 842d8bc, 26 rows on Linux and 26 on
      # Windows, both on a box that has `base` installed. A runner does not, so
      # its three base rows collapse to a single SKIP and 24 is the number to
      # expect here. The floor sits at 20 to leave a platform room to branch
      # differently -- macOS is the leg nobody on this repo can run by hand --
      # while still catching a board that halves.
      - name: Grade the scoreboard
        shell: bash
        run: |
          python scripts/grade-acceptance.py acceptance.json \
            --harness-rc "$(cat acceptance-rc.txt)" \
            --min-rows 20
